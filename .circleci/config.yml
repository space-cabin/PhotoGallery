version: 2.1
jobs:
  build:
    docker: 
      - image: circleci/node:4.8.2
    steps:
      - checkout
      - run: echo "installing dependencies"
      - run: npm install
      - run: echo "running test"
      - run: npm run test
      

